# Projet_Traitement_Images

pip install tensorflow opencv-python matplotlib numpy pillow seaborn scipy

# Mode démo (sans dataset)
python CR2_Pretraitement_Pipeline.py

# Avec le vrai dataset PlantVillage
# → Modifier use_sample=False dans le main